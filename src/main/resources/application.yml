spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/db_banco_sistema
    username: admin
    password: joao
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

